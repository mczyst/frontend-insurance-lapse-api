# Salin file ini ke .env.local untuk pengembangan lokal, atau isi sebagai
# Environment Variable di dashboard Vercel saat deploy.
# Wajib pakai prefix NEXT_PUBLIC_ agar bisa diakses dari browser (client-side).

NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/predict
